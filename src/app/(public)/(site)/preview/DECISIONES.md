# Decisiones de Daniela

## Aprobado
- Centrar el título principal de las pantallas "Baby Shower", "Colores" y "Baby Chuchi Lab" (antes estaba a la izquierda). Solo en computadora, en el celular queda igual que antes. → full-screen-sections.tsx (h1 "Baby Shower", h2 "Colores"), lab-full-screen.tsx (h2 "Baby Chuchi Lab"): added `md:text-center` to each title.
- En "Colores", bajar la notita crema para que quede centrada entre el título y la fila de círculos (antes estaba pegada arriba, junto al título), y agrandar un poco su letra. Solo en computadora. → full-screen-sections.tsx (ColoresFullScreen): wrapped the note box in a `FULL_SCREEN_FILL` + `md:flex md:items-center md:justify-center` spacer so the box itself keeps its natural size but is vertically centered in the leftover space above the swatches; text got `md:text-sm` (from `text-xs`).
- En "Colores", subir la fila de círculos para que no quede tan pegada abajo y esté más cerca de la notita. Solo en computadora. → full-screen-sections.tsx (ColoresFullScreen swatches row): changed `items-center` to `md:items-start` so the row sits at the top of its leftover-space area instead of vertically centered in it.
- Más aire entre el borde de arriba de la pantalla y el título, en "Baby Shower", "Colores" y "Baby Chuchi Lab" (estaba muy pegado arriba). Solo en computadora. → full-screen-sections.tsx (h1 "Baby Shower", h2 "Colores"), lab-full-screen.tsx (h2 "Baby Chuchi Lab"): added `md:mt-10` to each title.
- En "Baby Shower": el recuadro de la dirección vuelve a su posición original (más arriba), y la advertencia + botón "Lista de regalos" quedan pegados justo debajo de ese recuadro, con la letra un poco más grande, dejando el espacio libre restante al final de la pantalla. Solo en computadora. → full-screen-sections.tsx (EventInfoFullScreen): grouped the address box and the bottom row into one wrapper (`flex flex-col gap-4 md:gap-3`, tight gap between them); added two invisible spacer divs (`hidden` on mobile, `md:block` on desktop) — one between the title and the group, one after the group — so the flex-grow space that used to sit between the box and the row now splits between "above the box" and "below the row" instead of pushing the whole group to the bottom. Kept `md:text-sm` / `md:text-base` on the cash_note paragraph and the link.
- Subir un poco más el recuadro de la dirección, más cerca del título "Baby Shower" (ajustado en dos pasos). Solo en computadora. → full-screen-sections.tsx (EventInfoFullScreen): the top spacer's grow ratio went `md:flex-[3]` → `md:flex-[2]` → `md:flex-[1]` (bottom spacer stayed `md:flex-[4]`), pulling the box progressively closer to the title.
- En "Baby Chuchi Lab": achicar un poco el contador de semanas ("25 semanas"), y que las tarjetitas de los datos (hitos) no se estiren de punta a punta — que el recuadro blanco termine donde termina el texto. Solo en computadora. → lab-full-screen.tsx: added `md:text-xs` to the week counter (from `text-sm`); added `md:w-fit` to each milestone card so the white box hugs its content instead of stretching full width.
- Achicar y centrar la barra de progreso (con el piecito) del embarazo. Confirmado con Daniela: la fecha de última regla es 9 de febrero, y a la fecha de hoy el cálculo ya da 25 semanas correctamente — no hizo falta tocar esa cuenta. Solo en computadora. → lab-full-screen.tsx: added `md:mx-auto md:h-5 md:w-2/3` to the progress bar container (from `h-9` full width), `md:text-sm` to the foot emoji (from `text-lg`).
- Agrandar el texto "25 semanas" (después de haberlo achicado antes, Daniela pidió que fuera más grande). Solo en computadora. → lab-full-screen.tsx: week counter went `text-sm` (mobile) `md:text-xs` → `md:text-lg`.
- En "Baby Chuchi Lab": centrar el párrafo "Aunque compartimos carga genética..." como bloque (no solo el texto — el detallecito verde de la izquierda ahora viaja centrado junto con el texto en vez de quedar solo pegado al borde). Las tarjetitas de los hitos ahora arrancan alineadas con el borde izquierdo de la barra de progreso, y a medida que aparecen más (según avanzan las semanas) se acomodan en fila hacia la derecha, pasando a una segunda fila si no entran. Solo en computadora. → lab-full-screen.tsx: paragraph got `md:mx-auto md:w-fit md:max-w-xl` (replacing an earlier `md:text-center` that looked broken because the left accent bar stayed pinned to the far left); milestone list container got `md:mx-auto md:w-2/3` (matching the bar's width/centering) plus `md:flex-row md:flex-wrap md:content-start md:justify-start` (was a vertical stack).
- La foto de portada (se corta la cabecita del bebé), el espacio vacío arriba/abajo de la fila de círculos en "Colores", y el recuadro rojo de "Baby Shower" sin estirar a toda la altura: Daniela vio las tres cosas y pidió dejarlas tal cual están. Sin cambios de código.

## Rechazado
-

## Sin decidir
-

## Para Gustavo
-
